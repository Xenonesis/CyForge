id: challenge-005-windows-powershell
name: PowerShell Execution and Defense Evasion
description: PowerShell execution workflow and script-based payload staging lab.
domain: Python and Powershell for attack
content_type: independent
difficulty: medium
gitlab:
  repo_url: https://gitlab.example.com/cyberforge/cyberforge-content.git
  content_path: challenges/challenge-005-windows-powershell
tactic: execution
technique:
  attack_id: T1059.001
  name: PowerShell
platforms:
  windows:
    psh:
      command: '$RepoUrl = if ($env:REPO_URL) { $env:REPO_URL } else { "https://gitlab.example.com/cyberforge/cyberforge-content.git"
        }

        $WorkDir = "C:\\Temp\\cyberforge-challenge-005-windows-powershell"

        if (Test-Path $WorkDir) { Remove-Item -Recurse -Force $WorkDir }

        git clone --depth 1 $RepoUrl $WorkDir

        Set-Location "$WorkDir\\challenges\\challenge-005-windows-powershell"

        powershell -ExecutionPolicy Bypass -File .\\setup.ps1'
