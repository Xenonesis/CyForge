id: challenge-004-linux-privesc
name: Linux SUID Privilege Escalation
description: Escalate privileges through intentionally misconfigured SUID binaries.
domain: Linux OS attacks
content_type: independent
difficulty: medium
gitlab:
  repo_url: https://gitlab.example.com/cyberforge/cyberforge-content.git
  content_path: challenges/challenge-004-linux-privesc
tactic: privilege-escalation
technique:
  attack_id: T1548.001
  name: Setuid and Setgid
platforms:
  linux:
    sh:
      command: 'set -e

        REPO_URL="${REPO_URL:-https://gitlab.example.com/cyberforge/cyberforge-content.git}"

        WORKDIR="/tmp/cyberforge-challenge-004-linux-privesc"

        if [ -d "$WORKDIR" ]; then rm -rf "$WORKDIR"; fi

        git clone --depth 1 "$REPO_URL" "$WORKDIR"

        cd "$WORKDIR/challenges/challenge-004-linux-privesc"

        chmod +x setup.sh

        ./setup.sh'
