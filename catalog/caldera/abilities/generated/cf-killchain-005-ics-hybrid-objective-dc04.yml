- id: cf-killchain-005-ics-hybrid-objective-dc04
  name: CyberForge Deploy killchain-005-ics-hybrid-objective dc04
  description: Deploy machine dc04 from killchain-005-ics-hybrid-objective.
  tactic: execution
  technique:
    attack_id: T1059.001
    name: PowerShell
  platforms:
    windows:
      psh:
        command: '$RepoUrl = if ($env:REPO_URL) { $env:REPO_URL } else { "https://gitlab.example.com/cyberforge/cyberforge-content.git"
          }

          $WorkDir = "C:\\Temp\\cyberforge-killchain-005-ics-hybrid-objective-dc04"

          if (Test-Path $WorkDir) { Remove-Item -Recurse -Force $WorkDir }

          git clone --depth 1 $RepoUrl $WorkDir

          Set-Location "$WorkDir\\killchains\\killchain-005-ics-hybrid-objective\\dc04"

          powershell -ExecutionPolicy Bypass -File .\\dc04_setup.ps1'
