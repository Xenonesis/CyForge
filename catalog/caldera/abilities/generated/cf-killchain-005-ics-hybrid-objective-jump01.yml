- id: cf-killchain-005-ics-hybrid-objective-jump01
  name: CyberForge Deploy killchain-005-ics-hybrid-objective jump01
  description: Deploy machine jump01 from killchain-005-ics-hybrid-objective.
  tactic: execution
  technique:
    attack_id: T1059.001
    name: PowerShell
  platforms:
    windows:
      psh:
        command: '$RepoUrl = if ($env:REPO_URL) { $env:REPO_URL } else { "https://gitlab.example.com/cyberforge/cyberforge-content.git"
          }

          $WorkDir = "C:\\Temp\\cyberforge-killchain-005-ics-hybrid-objective-jump01"

          if (Test-Path $WorkDir) { Remove-Item -Recurse -Force $WorkDir }

          git clone --depth 1 $RepoUrl $WorkDir

          Set-Location "$WorkDir\\killchains\\killchain-005-ics-hybrid-objective\\jump01"

          powershell -ExecutionPolicy Bypass -File .\\jump01_setup.ps1'
