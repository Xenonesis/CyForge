- id: cf-killchain-002-phishing-to-adcs-app02
  name: CyberForge Deploy killchain-002-phishing-to-adcs app02
  description: Deploy machine app02 from killchain-002-phishing-to-adcs.
  tactic: execution
  technique:
    attack_id: T1059.004
    name: Unix Shell
  platforms:
    linux:
      sh:
        command: 'set -e

          REPO_URL="${REPO_URL:-https://gitlab.example.com/cyberforge/cyberforge-content.git}"

          WORKDIR="/tmp/cyberforge-killchain-002-phishing-to-adcs-app02"

          if [ -d "$WORKDIR" ]; then rm -rf "$WORKDIR"; fi

          git clone --depth 1 "$REPO_URL" "$WORKDIR"

          cd "$WORKDIR/killchains/killchain-002-phishing-to-adcs/app02"

          chmod +x app02_setup.sh

          ./app02_setup.sh'
