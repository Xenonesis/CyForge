- id: cf-killchain-004-supply-chain-domain-dc03
  name: CyberForge Deploy killchain-004-supply-chain-domain dc03
  description: Deploy machine dc03 from killchain-004-supply-chain-domain.
  tactic: execution
  technique:
    attack_id: T1059.001
    name: PowerShell
  platforms:
    windows:
      psh:
        command: '$RepoUrl = if ($env:REPO_URL) { $env:REPO_URL } else { "https://gitlab.example.com/cyberforge/cyberforge-content.git"
          }

          $WorkDir = "C:\\Temp\\cyberforge-killchain-004-supply-chain-domain-dc03"

          if (Test-Path $WorkDir) { Remove-Item -Recurse -Force $WorkDir }

          git clone --depth 1 $RepoUrl $WorkDir

          Set-Location "$WorkDir\\killchains\\killchain-004-supply-chain-domain\\dc03"

          powershell -ExecutionPolicy Bypass -File .\\dc03_setup.ps1'
