- id: cf-killchain-005-ics-hybrid-objective-web04
  name: CyberForge Deploy killchain-005-ics-hybrid-objective web04
  description: Deploy machine web04 from killchain-005-ics-hybrid-objective.
  tactic: execution
  technique:
    attack_id: T1059.004
    name: Unix Shell
  platforms:
    linux:
      sh:
        command: 'set -e

          REPO_URL="${REPO_URL:-https://gitlab.example.com/cyberforge/cyberforge-content.git}"

          WORKDIR="/tmp/cyberforge-killchain-005-ics-hybrid-objective-web04"

          if [ -d "$WORKDIR" ]; then rm -rf "$WORKDIR"; fi

          git clone --depth 1 "$REPO_URL" "$WORKDIR"

          cd "$WORKDIR/killchains/killchain-005-ics-hybrid-objective/web04"

          chmod +x web04_setup.sh

          ./web04_setup.sh'
