- id: cf-killchain-003-vpn-breach-ransomware-files01
  name: CyberForge Deploy killchain-003-vpn-breach-ransomware files01
  description: Deploy machine files01 from killchain-003-vpn-breach-ransomware.
  tactic: execution
  technique:
    attack_id: T1059.001
    name: PowerShell
  platforms:
    windows:
      psh:
        command: '$RepoUrl = if ($env:REPO_URL) { $env:REPO_URL } else { "https://gitlab.example.com/cyberforge/cyberforge-content.git"
          }

          $WorkDir = "C:\\Temp\\cyberforge-killchain-003-vpn-breach-ransomware-files01"

          if (Test-Path $WorkDir) { Remove-Item -Recurse -Force $WorkDir }

          git clone --depth 1 $RepoUrl $WorkDir

          Set-Location "$WorkDir\\killchains\\killchain-003-vpn-breach-ransomware\\files01"

          powershell -ExecutionPolicy Bypass -File .\\files01_setup.ps1'
