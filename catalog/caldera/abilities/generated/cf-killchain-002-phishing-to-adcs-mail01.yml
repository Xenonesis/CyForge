- id: cf-killchain-002-phishing-to-adcs-mail01
  name: CyberForge Deploy killchain-002-phishing-to-adcs mail01
  description: Deploy machine mail01 from killchain-002-phishing-to-adcs.
  tactic: execution
  technique:
    attack_id: T1059.004
    name: Unix Shell
  platforms:
    linux:
      sh:
        command: 'set -e

          REPO_URL="${REPO_URL:-https://gitlab.example.com/cyberforge/cyberforge-content.git}"

          WORKDIR="/tmp/cyberforge-killchain-002-phishing-to-adcs-mail01"

          if [ -d "$WORKDIR" ]; then rm -rf "$WORKDIR"; fi

          git clone --depth 1 "$REPO_URL" "$WORKDIR"

          cd "$WORKDIR/killchains/killchain-002-phishing-to-adcs/mail01"

          chmod +x mail01_setup.sh

          ./mail01_setup.sh'
