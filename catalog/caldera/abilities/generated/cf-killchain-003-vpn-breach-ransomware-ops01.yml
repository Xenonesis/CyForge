- id: cf-killchain-003-vpn-breach-ransomware-ops01
  name: CyberForge Deploy killchain-003-vpn-breach-ransomware ops01
  description: Deploy machine ops01 from killchain-003-vpn-breach-ransomware.
  tactic: execution
  technique:
    attack_id: T1059.004
    name: Unix Shell
  platforms:
    linux:
      sh:
        command: 'set -e

          REPO_URL="${REPO_URL:-https://gitlab.example.com/cyberforge/cyberforge-content.git}"

          WORKDIR="/tmp/cyberforge-killchain-003-vpn-breach-ransomware-ops01"

          if [ -d "$WORKDIR" ]; then rm -rf "$WORKDIR"; fi

          git clone --depth 1 "$REPO_URL" "$WORKDIR"

          cd "$WORKDIR/killchains/killchain-003-vpn-breach-ransomware/ops01"

          chmod +x ops01_setup.sh

          ./ops01_setup.sh'
