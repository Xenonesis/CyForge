- id: cf-killchain-003-vpn-breach-ransomware-ws03
  name: CyberForge Deploy killchain-003-vpn-breach-ransomware ws03
  description: Deploy machine ws03 from killchain-003-vpn-breach-ransomware.
  tactic: execution
  technique:
    attack_id: T1059.001
    name: PowerShell
  platforms:
    windows:
      psh:
        command: '$RepoUrl = if ($env:REPO_URL) { $env:REPO_URL } else { "https://gitlab.example.com/cyberforge/cyberforge-content.git"
          }

          $WorkDir = "C:\\Temp\\cyberforge-killchain-003-vpn-breach-ransomware-ws03"

          if (Test-Path $WorkDir) { Remove-Item -Recurse -Force $WorkDir }

          git clone --depth 1 $RepoUrl $WorkDir

          Set-Location "$WorkDir\\killchains\\killchain-003-vpn-breach-ransomware\\ws03"

          powershell -ExecutionPolicy Bypass -File .\\ws03_setup.ps1'
