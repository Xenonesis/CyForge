- id: cf-killchain-004-supply-chain-domain-deploy01
  name: CyberForge Deploy killchain-004-supply-chain-domain deploy01
  description: Deploy machine deploy01 from killchain-004-supply-chain-domain.
  tactic: execution
  technique:
    attack_id: T1059.001
    name: PowerShell
  platforms:
    windows:
      psh:
        command: '$RepoUrl = if ($env:REPO_URL) { $env:REPO_URL } else { "https://gitlab.example.com/cyberforge/cyberforge-content.git"
          }

          $WorkDir = "C:\\Temp\\cyberforge-killchain-004-supply-chain-domain-deploy01"

          if (Test-Path $WorkDir) { Remove-Item -Recurse -Force $WorkDir }

          git clone --depth 1 $RepoUrl $WorkDir

          Set-Location "$WorkDir\\killchains\\killchain-004-supply-chain-domain\\deploy01"

          powershell -ExecutionPolicy Bypass -File .\\deploy01_setup.ps1'
